import * as React from 'react';

import PlaceHolder from 'components/PlaceHolder';

class Body extends React.Component<{}, {}> {
  public render() {
    return <PlaceHolder>Waiting for content </PlaceHolder>;
  }
}

export default Body;
